import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.addy79.kali',
  appName: 'Kali Chess',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config